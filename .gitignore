# build artifacts
build/
imagedecoder.svg/addon.xml

# commonly used editors
*.swp
*.project
*.cproject
.classpath
*.sublime-*
.settings/
*.kdev4
*~
/.idea
.idea/
/*.patch
.vs/

# General MacOS
.AppleDouble
.DS_Store
.LSOverride

# local-only working files, never part of the addon
# (also listed in .git/info/exclude, but that is not shared and is lost on a
#  fresh clone - keep them here too now that this repo is public)
/.dev/
/CLAUDE.md
